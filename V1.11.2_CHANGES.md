# v1.11.2 CHANGES

## 本版修正

1. 旅程工具由手機 2 欄改為 3 欄
   - 7 個工具改為 3 + 3 + 1 排列
   - 同步縮減工具卡高度、emoji 與文字尺寸，避免畫面拉太長

2. 手動匯率顯示精度修正
   - 輸入 0.215 時，結果附註會顯示 `1 JPY = 0.215 TWD`
   - 不再錯誤四捨五入成 0.22
   - 金額結果仍維持適合閱讀的 0～2 位小數

3. 訂位／票券新增功能
   - 訂位票券頁右上新增 `＋` 按鈕
   - 可新增：類型、名稱、時間、處理期限、備註、地點
   - 自訂項目可標記完成、刪除
   - 自訂項目會保存本機，登入 Firebase 時同步到 `bookingItems`

## 資料
- 不需重新匯入 content seed。
- 新增訂位會寫入獨立動態節點 `bookingItems`，不會修改 D1–D9 靜態行程。
