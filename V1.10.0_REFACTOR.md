# v1.10.0｜離線旅行包＋程式瘦身重構

## 1. 離線旅行包

設定 →「離線旅行包」可下載完整旅行圖片素材。

下載內容共 38 項，約 14 MB：
- D1–D9 中文主圖：9 張
- D1–D9 日文主圖：9 張
- Decision 圖：6 張
- 天氣圖：5 張
- 紅葉狀態圖：5 張
- Empty State 圖：4 張

下載採手動觸發，避免第一次開啟 App 就下載十多 MB。下載中有進度，未完成可以續傳式補齊，完成後可以更新或移除。

Service Worker 使用獨立 `kyushu-nov-offline-pack-v1` Cache Storage。更新 App shell 時不會刪掉這個離線包。

私人行程 JSON 不放進公開靜態檔；只有已授權且成功載入過行程的裝置，才會保留本機快取供離線啟動。

## 2. 快取架構

- `kyushu-nov-shell-v1.10.0`：App shell 與首頁必要素材
- `kyushu-nov-runtime-v1.10.0`：使用途中取得的同源資源
- `kyushu-nov-offline-pack-v1`：使用者手動下載的完整旅行素材

Service Worker activation 只清理 `kyushu-nov-shell-*` 與 `kyushu-nov-runtime-*` 的舊版本；不碰 10 月專案 cache，也不碰手動離線包。

## 3. 真正移除的舊程式

- Pompompurin / Usagi / Buddy 主題邏輯
- Buddy reaction / peek / dialogue
- Secret Life 彩蛋引擎與預載
- Buddy Hero easter egg
- 舊 weather Usagi controller
- Chapter 導覽、Chapter lightbox、Chapter event handlers
- 舊 Buddy booking / event character 動畫
- 已無用途的相關 CSS selectors 與圖檔引用

`purin-food-ui.png` 已從完整版本移除。

## 4. 程式體積整理

以 v1.9.12 為比較基準：
- `app.js`：約 162 KB → 約 89 KB
- `style.css`：約 267 KB → 約 100 KB
- Hero 的歷代 WA 覆寫規則已收斂成單一最終規則，避免後續再發生「新 CSS 被舊 CSS 蓋掉」的問題。
- maskable icon 重新無損／高品質壓縮，約 933 KB → 約 302 KB。

實際 ZIP 大小請以本版打包結果為準。

## 5. 離線模式仍有的限制

離線旅行包不是把整個網際網路搬進 App：
- Google Maps、Google 搜尋、餐廳官方網站等外部網站仍需要網路。
- 即時天氣資料需要網路；曾取得的畫面／本機資料可依現有快取策略使用，但不會在離線時產生新的預報。
- Firebase 即時同步需要網路；記事、購物、花費等既有本機狀態可繼續使用，重新連線後再同步。
- 第一次使用一定要先在線上完成 Google 授權並成功讀取私人行程；這是刻意保留的隱私／權限設計。

## 6. 建議測試

1. 線上登入並正常進入 App。
2. 設定 → 離線旅行包 → 下載完整離線包，確認顯示 38/38。
3. 開啟幾張 D1–D9 中／日文圖與 Decision 圖。
4. 關閉 Wi‑Fi / 行動網路，完全關掉 App 後重新開啟。
5. 確認私人行程、每日圖、Decision、天氣圖樣與紅葉素材可開啟。
6. 恢復網路，確認 Firebase 同步與即時天氣恢復。
