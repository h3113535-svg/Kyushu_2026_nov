# v1.11.0｜Tools & Readability Pass

## 修正
- 修正行程卡長按後「旅伴小攻略」標題與段落異常換行：移除 Buddy 拿掉後殘留的 58px 空白 grid 欄。
- Guide 文字改用適合繁中段落的換行規則，避免兩三個字被擠成狹長直排。
- 每日 D1–D9 主圖 lightbox 新增 100%～400% 縮放。
- 支援 ＋／−／100% 重設、雙擊放大、雙指縮放；放大後可拖曳，100% 時才左右滑換天。

## 旅程工具
- 紅葉・銀杏觀測從每日行程頁移至「旅程工具 → 紅葉觀測」，預設進工具時顯示全旅程。
- 新增「匯率計算」：手動輸入 1 JPY = ? TWD；支援日圓算式 + − × ÷ ()，並提供 TWD 反算 JPY。匯率只存本機、旅程 namespace 隔離。
- 新增「Google Maps 地點匯入」：可貼分享文字／完整 Google Maps URL／maps.app.goo.gl 短網址；自動解析可取得的名稱與連結，安排 D1–D9、分類與備註後保存。
- 匯入地點使用 `mapPlaces` 動態 Firebase root，同步方式與原本 foods/shopping/expenses 相同；無需重新匯入 content seed。
- Maps 行為維持 search/place-first，不產生 directions URL。

## 注意
- Google Maps 短網址本身不含可解析店名，而且瀏覽器端不可靠跨站展開；若只貼短網址，請補一個名稱。
- Firebase 不需重新匯入 seed；第一次新增地點時會自動建立 `trips/kyushu-nov-2026/mapPlaces`。
